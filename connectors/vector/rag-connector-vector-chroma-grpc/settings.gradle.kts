rootProject.name = "rag-connector-vector-chroma-grpc"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
    plugins {
        kotlin("jvm")
    }
}