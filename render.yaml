services:
  - type: web
    name: ner-demo
    runtime: image
    image:
      url: ghcr.io/pimpmyrag/ner-demo:latest
    healthCheckPath: /
    port: 10000
    plan: standard
    envVars:
      - key: JAVA_TOOL_OPTIONS
        # Heap réduit à 1g : le modèle ONNX (708MB) est en mémoire native (off-heap).
        # Budget mémoire Standard (2GB) : 1GB heap + 708MB ONNX + ~200MB OS/JVM ≈ 1.9GB
        value: -Xms128m -Xmx1g
