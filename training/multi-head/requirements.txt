# Core training dependencies
# torch>=2.6.0 requis : transformers bloque torch.load() < 2.6 (CVE-2025-32434)
# Sur RunPod CUDA 12.x : pip install "torch>=2.6.0" --index-url https://download.pytorch.org/whl/cu121
# torch est fourni par l'image RunPod (2.4+cu124) — ne pas upgrader (torchvision serait incompatible)
# torch>=2.0.0
# v9-modernbert : bump 4.46.2 → 4.53.0 pour supporter answerdotai/ModernBERT-base
# (ModernBERT ajouté en 4.48.0). 4.53.0 corrige des ReDoS vs 4.46.2, aucun CVE
# nouveau. NE PAS passer à 5.x (breaking API). DeBERTa-v3 reste supporté.
transformers==4.53.0
tokenizers>=0.21.0
sentencepiece>=0.2.0
scikit-learn>=1.3.0
numpy>=1.24.0
protobuf>=4.0.0
torch>=2.6.0

# Experiment tracking
wandb>=0.17.0

# Dataset versioning (pull datasets uniquement — pas de dvc add/push depuis le pod)
dvc>=3.0.0
dvc-s3>=3.0.0   # backend S3 pour dvc pull

# boto3 est déjà dans l'image RunPod — pas besoin d'awscli (lourd)
# L'upload R2 se fait via boto3 directement (voir setup_runpod.sh)

# RunPod SDK (lancement pods depuis le local)
runpod>=1.9.0
python-dotenv>=1.0.0
stanza>=1.8.2

# Optional: for ONNX export
# onnxruntime>=1.17.0

