# Core training dependencies
# torch>=2.6.0 requis : transformers bloque torch.load() < 2.6 (CVE-2025-32434)
# Sur RunPod CUDA 12.x : pip install "torch>=2.6.0" --index-url https://download.pytorch.org/whl/cu121
torch>=2.6.0
transformers>=4.40.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
scikit-learn>=1.3.0
numpy>=1.24.0
protobuf>=4.0.0

# Experiment tracking
wandb>=0.17.0

# Dataset versioning (remote storage)
dvc>=3.0.0
dvc-s3>=3.0.0   # backend S3 — remplacer par dvc-gs pour GCS, ou retirer si remote SSH/local

# RunPod SDK (lancement pods depuis le local)
runpod>=1.9.0
python-dotenv>=1.0.0

# Optional: for ONNX export
# onnxruntime>=1.17.0

