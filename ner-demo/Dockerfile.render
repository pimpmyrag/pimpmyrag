# Render-specific Dockerfile: just pulls the pre-built image from GHCR.
# The real build (Gradle + model download) happens in GitHub Actions.
# This file is auto-updated by CI (build-push-ner-demo.yml) after each push.
FROM ghcr.io/pimpmyrag/ner-demo:sha-1647ca9
